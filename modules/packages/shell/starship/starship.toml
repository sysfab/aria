add_newline = false

format = """
$directory\
$character"""

[character]
success_symbol = "[](bold green)"
error_symbol = "[](bold red)"

[directory]
style = "blue"
truncation_length = 4
truncate_to_repo = false
truncation_symbol = '.../'
